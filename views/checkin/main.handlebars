<!DOCTYPE html>
<html>
<head>
  <title>Welcome</title>

  {{>styles}}

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>

  <style>
  footer {
    position: fixed;
    bottom: 0;
    width: 100%;
    height: 50px;
    background-color: #071C33;
  }
  footer h4 {
    color: #eeeeee;
    text-align: center;
    padding: 0;
    margin: 0;
    line-height: 50px;
    font-weight: bold;
    text-shadow: 0px 0px 8px #ffae00;
  }
  </style>


</head>

<body class = "customBG">

  <h1 class ="header text-center"><strong>Welcome to {{companyName}}!</strong></h1>

  <img class="checkinphoto center-block" src="{{ logo }}" alt="...">

  <div class="row">
    <div class="col-xs-10 col-sm-6 col-md-6 col-lg-4 center-block" style="float:none; padding: 20px">
      {{#if error}}
      <div class="alert alert-danger">{{error}}</div>
      {{/if}}
      <form action="/office/{{ businessId }}/checkin">
        <button type="submit" class="btn btn-primary btn-lg btn-block">Check In</button>
      </form>
      <form action="/office/{{ businessId }}/appointments">
        <button type="submit" class="btn btn-primary btn-lg btn-block">Set Up Appointment</button>
      </form>
    </div>
  </div>

  <footer>
    <h4>Powered by ENQUÉ</h4>
  </footer>

</body>
</html>
