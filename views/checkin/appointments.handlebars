<!DOCTYPE html>
<html>
<head>
    <title>Welcome</title>

    {{>styles}}

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.2/js/bootstrap.min.js"></script>

    <style>

    footer {
        position: fixed;
        bottom: 0;
        width: 100%;
        height: 50px;
        background-color: #071C33;
    }
    footer h4 {
        color: #eeeeee;
        text-align: center;
        padding: 0;
        margin: 0;
        line-height: 50px;
        font-weight: bold;
        text-shadow: 0px 0px 8px #ffae00;
    }
    </style>


</head>

<body class = "customBG">

<h1 class ="header text-center"><strong>Welcome to {{companyName}}!</strong></h1>



<!--<img class="checkinphoto center-block" src="{{logo}}" alt="...">-->

<div class="row">
    <div class="col-xs-10 col-sm-6 col-md-6 col-lg-4 center-block" style="float:none; padding: 20px">
        <div class="box box-primary checkin-box">
            <div class="box-header with-border">
                <h3 class="box-title">Set Up an Appointment</h3>
            </div>
            {{#if error}}
                <div class="alert alert-danger">{{error}}</div>
            {{/if}}
            <div class="box-body">
              <form method="post" action="/office/{{ businessId }}/appointments" enctype="application/x-www-form-urlencoded">
                <div class="form-group">
                  <label for="customerFirstName"> Customer's First Name </label>
                  <input type="text" id="customerFirstName" name="customerFirstName" value="{{ customerFirstName }}" class="form-control" required>
                  <label for="customerLastName"> Customer's Last Name </label>
                  <input type="text" id="customerLastName" name="customerLastName" value="{{ customerLastName }}" class="form-control" required>
                  <label for="customerAge"> Customer's Age </label>
                  <input type="text" id="customerAge" name="customerAge" value="{{ customerAge }}" class="form-control" required>
                  <label for="aptTime"> Appointment Time </label>
                  <input type="date" id="aptTime" name="aptTime" value="{{ aptTime }}" class="form-control" required>
                </div>
                <input class="btn btn-block btn-primary btn-lg btn-flat" name="submit" type="submit" href="/appointments" value="Add Appointment">
              </form>
            </div>
        </div>
    </div>
</div>

<footer>
    <h4>Powered by ENQUÉ</h4>
</footer>

</body>
</html>
